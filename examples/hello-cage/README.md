# Hello Cage

Synthetic MIT showcase for the complete public path:

`GitHub issue -> immutable raw manifest -> Summon Chant -> local save -> local
Drill -> mutable prototype handoff -> offline use`.

`manifest.json` is generated only after the source files are committed, so each
dimension URL names the exact source commit. It is not hand-edited.
