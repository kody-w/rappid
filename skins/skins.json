{
  "claude":      { "command": "claude -p {prompt}",
                   "notes": "Claude Code CLI, non-interactive print mode" },
  "copilot":     { "command": "copilot -p {prompt} --allow-all-tools",
                   "notes": "GitHub Copilot CLI, non-interactive" },
  "openrappter": { "command": "openrappter ask {prompt}",
                   "notes": "OpenRappter local agent" },
  "brainstem":   { "command": "curl -s -X POST http://127.0.0.1:7071/chat -H 'Content-Type: application/json' -d '{\"user_input\": {prompt}}'",
                   "notes": "already speaks the seam natively — skin adds only the species voice; prefer attaching the brainstem directly" },
  "opengrokbot": { "command": null, "notes": "fill in when its CLI lands" },
  "openclaw":    { "command": null, "notes": "fill in when its CLI lands" },
  "hermes":      { "command": null, "notes": "fill in when its CLI lands" }
}
